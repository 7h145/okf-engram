---
description: Use Engram project, global, and linked knowledge bases
argument-hint: "[request]"
---
Activate and follow the `okf-engram` skill before making any tool call. Treat this
as a strict `/engram` invocation and pass the exact arguments below to the skill's
**Strict request preflight**. `SKILL.md` is the sole authority for command grammar,
routing, safety policy, and workflows; this Pi prompt does not parse or reinterpret
the request.

Engram request:
$ARGUMENTS
